pip install --upgrade pip setuptools wheel
pip install playsound

# to install pygobject
sudo apt update
sudo apt install pkg-config libcairo2-dev python3-dev libgirepository1.0-dev

pip install pygobject
